# Server
PORT=3001
NODE_ENV=development

# JWT
JWT_SECRET=change-me-to-a-random-secret-at-least-32-chars
JWT_EXPIRES_IN=24h

# Encryption key for credentials (32 bytes hex = 64 chars)
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Database
# Development: file:./dev.db
# Docker:      file:./data/idp.db (mounted volume, set automatically by docker-compose)
DATABASE_URL=file:./dev.db

# Server URL (used for federation callback URLs)
# Set this to your public-facing URL in production (e.g., https://portal.example.com)
# SERVER_URL=http://localhost:3001

# Client URL (for CORS — only needed if client is served from a different origin)
# In Docker/production the server serves the SPA directly, so this is not required.
# CLIENT_URL=http://localhost:5173

# GitHub OAuth (optional)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Terraform binary path (optional, defaults to 'terraform' on PATH)
# TERRAFORM_BIN=/usr/local/bin/terraform
