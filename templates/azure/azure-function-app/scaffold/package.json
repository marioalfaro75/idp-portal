{
  "name": "{{service_name}}",
  "version": "1.0.0",
  "description": "{{service_name}} Azure Function",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "start": "func start",
    "dev": "npm run build && func start"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
