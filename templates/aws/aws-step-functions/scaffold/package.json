{
  "name": "{{service_name}}",
  "version": "1.0.0",
  "description": "{{service_name}} Step Functions workflow",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../validate.zip validate.js && zip -r ../process.zip process.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "typescript": "^5.3.3"
  }
}
