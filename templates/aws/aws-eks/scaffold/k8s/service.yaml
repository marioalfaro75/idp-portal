apiVersion: v1
kind: Service
metadata:
  name: {{service_name}}
  labels:
    app: {{service_name}}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: {{service_name}}
